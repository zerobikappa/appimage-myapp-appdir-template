# 由于我把我自己电脑的默认xdg user dirs 里的名字改成小写了，导致生成的exeinfo里包含的路径不正确，只有我自己能正常使用 -s 功能，其他人不行。(fixed)
# 无法读取连在一起的短选项(fixed)
# 增加proton支持(done)
# proton模式无法使用全通关存档(fixed)
# 需要更改savedata restore的机制，删除原本是没有的文件。(fixed)
#
#
# 无论是wine模式还是proton模式，第一次(有时也不一定是第一次)生成prefix时会有读取错误的情况发生
# 要增加lock file机制。现有的机制有问题，打包成appimage包后运行仍然可以多开。
# 如果设置了-s，在复制存档前需要先检查savedata dir路径是否为空。


